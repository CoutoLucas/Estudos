let = x1 = 1, x2 = 1, y1 = 1, y2 = 4, distancia = 0;
distancia = Math.sqrt(Math.pow(x1 - x2, 2) + (Math.pow(y1 - y2, 2)));
console.log(`A distancia entre estes dois pontos é ${distancia.toFixed(2)} unidades de distancia`);